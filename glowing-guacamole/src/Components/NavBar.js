import React from "react";

function NavBar() {
    return(
        <div>
            <nav>
                <a href="#">Home</a>
                <a href="#">Sign In</a>
                <a href="#">Sign Up</a>
            </nav>
        </div>
    )
}

export default Navbar;
